# React Native (Expo) Conventions

- **Expo SDK 52** — use Expo APIs where possible
- **Navigation**: React Navigation (bottom-tabs, native-stack)
- **State**: Context (`AppContext`) for auth/profile
- **API**: Shared api-client pattern; config for base URL
- **Screens**: `src/screens/`; components in `src/components/`
